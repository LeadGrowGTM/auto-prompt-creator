---
phase: 02-execution-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenarios/icp-classification/prompts/v001.md
  - scenarios/icp-classification/mutations.log
  - scenarios/icp-classification/evals/v001-raw.json
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, TRACK-01, TRACK-02]

must_haves:
  truths:
    - "v001.md contains a complete candidate prompt with task instruction, output schema, rules, and input placeholder"
    - "Haiku produces structured JSON output for each of the 9 inputs when given the candidate prompt"
    - "Parse failures are caught and scored as 0 without crashing — the run processes all 9 inputs regardless"
    - "Raw Haiku outputs are stored in a structured file for the judge to consume"
    - "mutations.log exists with the v001 initial entry"
  artifacts:
    - path: "scenarios/icp-classification/prompts/v001.md"
      provides: "First candidate prompt for Haiku"
      contains: "Return ONLY a JSON object"
    - path: "scenarios/icp-classification/evals/v001-raw.json"
      provides: "Raw Haiku outputs for all 9 inputs"
      contains: "haiku_output"
    - path: "scenarios/icp-classification/mutations.log"
      provides: "Append-only mutation history"
      contains: "v001 (initial)"
  key_links:
    - from: "scenarios/icp-classification/prompts/v001.md"
      to: "CC Agent tool with model=haiku"
      via: "prompt passed inline to agent"
      pattern: "candidate prompt.*input"
    - from: "scenarios/icp-classification/ground-truth/*.json"
      to: "scenarios/icp-classification/evals/v001-raw.json"
      via: "input field read from ground truth, passed to Haiku"
      pattern: "input_ref"
---

<objective>
Create the first candidate prompt (v001.md), execute it against Haiku for all 9 inputs via CC agent spawning, capture and parse structured JSON outputs, handle parse failures gracefully, and store raw results for the judge. Establish the prompt versioning and mutation log formats.

Purpose: This is the execution engine — the ability to run any candidate prompt against the target model and collect structured results. Phase 3's mutation loop will call this repeatedly.
Output: v001.md (candidate prompt), v001-raw.json (Haiku outputs for all 9 inputs), mutations.log (initial entry)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-execution-evaluation/02-CONTEXT.md
@.planning/phases/02-execution-evaluation/02-RESEARCH.md
@scenarios/icp-classification/scenario.json
@scenarios/icp-classification/README.md

<interfaces>
<!-- Ground truth file structure (read input from these, pass to Haiku) -->

From scenarios/icp-classification/ground-truth/abacus-ai.json:
```json
{
  "id": "abacus-ai",
  "input": {
    "company_name": "Abacus AI",
    "company_description": "Enterprise AI platform...",
    "website_url": "https://abacus.ai"
  },
  "expected_output": { ... },
  "split": "train"
}
```

From scenarios/icp-classification/scenario.json:
```json
{
  "output_schema": {
    "industry": { "type": "string" },
    "company_size": { "type": "string", "allowed_values": ["startup", "SMB", "mid-market", "enterprise"] },
    "decision_makers": { "type": "string[]" },
    "pain_points": { "type": "string[]" },
    "icp_fit": { "type": "string", "allowed_values": ["strong", "moderate", "weak"] },
    "reasoning": { "type": "string" }
  },
  "rubric": {
    "dimensions": {
      "accuracy": { "weight": 0.4 },
      "label_normalization": { "weight": 0.25 },
      "completeness": { "weight": 0.2 },
      "specificity": { "weight": 0.15 }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v001 candidate prompt, mutations.log, and establish file formats</name>
  <files>scenarios/icp-classification/prompts/v001.md, scenarios/icp-classification/mutations.log</files>
  <read_first>
    - scenarios/icp-classification/scenario.json (output schema, rubric, label_definitions, config)
    - scenarios/icp-classification/ground-truth/dbs-building-solutions.json (example of expected casual language style)
    - scenarios/icp-classification/ground-truth/abacus-ai.json (example of weak fit classification)
  </read_first>
  <action>
Create `scenarios/icp-classification/prompts/v001.md` with this exact content (per D-01, D-02, D-11):

```
You are classifying a company into ICP segments. Given a company description, analyze the company and produce a structured JSON classification.

## Output Format

Return ONLY a JSON object with these exact fields. No other text, no markdown code fences, no explanation before or after:

{
  "industry": "casual industry label (e.g. 'commercial cleaning' not 'janitorial services')",
  "company_size": "startup | SMB | mid-market | enterprise",
  "decision_makers": ["casual title 1", "casual title 2"],
  "pain_points": ["specific pain 1", "specific pain 2", "specific pain 3"],
  "icp_fit": "strong | moderate | weak",
  "reasoning": "One sentence explaining the classification"
}

## Rules

- Use casual business language. Write "CMOs" not "Chief Marketing Officers". Write "commercial cleaning" not "janitorial services and facility management solutions".
- company_size: startup = pre-revenue or <20 employees. SMB = 20-200 employees. mid-market = 200-2000 employees. enterprise = 2000+ employees.
- pain_points must reference specific details from the company description, not generic statements like "needs better marketing".
- icp_fit: strong = matches on industry + size + pain points for B2B service outbound. moderate = matches 1-2 dimensions. weak = does not match key dimensions.
- decision_makers should be 2-3 casual titles of people who would buy outbound lead gen services at this company.

## Company to Classify
```

Create `scenarios/icp-classification/mutations.log` with this exact content (per D-13):

```
## v001 (initial)
Created: [executor fills timestamp]
Source: Manual baseline prompt
Train score: pending
Validation score: pending
```
  </action>
  <verify>
    <automated>cat scenarios/icp-classification/prompts/v001.md | head -5</automated>
    <automated>cat scenarios/icp-classification/mutations.log | head -3</automated>
  </verify>
  <acceptance_criteria>
    - prompts/v001.md exists and contains "Return ONLY a JSON object"
    - prompts/v001.md contains "Company to Classify" as the last line
    - prompts/v001.md contains all 6 output fields: industry, company_size, decision_makers, pain_points, icp_fit, reasoning
    - prompts/v001.md contains the label_definitions: "startup = pre-revenue or <20 employees"
    - prompts/v001.md does NOT contain markdown code fences around the JSON template (the template is shown as plain text example)
    - mutations.log exists and contains "## v001 (initial)"
    - mutations.log contains "Train score: pending"
  </acceptance_criteria>
  <done>v001.md is a complete candidate prompt ready to pass to Haiku. mutations.log has the initial entry.</done>
</task>

<task type="auto">
  <name>Task 2: Execute v001 against Haiku for all 9 inputs and store raw results</name>
  <files>scenarios/icp-classification/evals/v001-raw.json</files>
  <read_first>
    - scenarios/icp-classification/prompts/v001.md (the candidate prompt to send)
    - scenarios/icp-classification/scenario.json (output_schema for field validation)
    - scenarios/icp-classification/ground-truth/abacus-ai.json (input structure, split field)
    - scenarios/icp-classification/ground-truth/bartos-industries.json
    - scenarios/icp-classification/ground-truth/dbs-building-solutions.json
    - scenarios/icp-classification/ground-truth/dealer-teamwork.json
    - scenarios/icp-classification/ground-truth/osp.json
    - scenarios/icp-classification/ground-truth/roush-cleantech.json
    - scenarios/icp-classification/ground-truth/title-one.json
    - scenarios/icp-classification/ground-truth/trx-services.json
    - scenarios/icp-classification/ground-truth/wisconsin-plastics.json
  </read_first>
  <action>
For each of the 9 ground truth files, execute the candidate prompt against Haiku (per D-01, D-02, D-03):

1. Read the ground truth file. Extract the `input` object (company_name, company_description, website_url).
2. Read v001.md. Append the input as: `\n\n{company_name}: {company_description}`
3. Spawn a CC Agent with model="haiku". Pass the combined prompt+input as the agent instruction. The agent should return the JSON classification.
4. Capture the agent's response as raw text.
5. Parse the response:
   - Strip any markdown code fences (```json ... ```) if present
   - Trim leading/trailing whitespace and any non-JSON text
   - Attempt JSON.parse on the cleaned text
   - If parse succeeds: validate that all 6 fields exist (industry, company_size, decision_makers, pain_points, icp_fit, reasoning)
   - If parse fails OR fields missing: mark as `parse_failure: true`, store the raw text, set `haiku_output: null`
6. Per D-03: Never crash or halt on parse failure. Log it and continue to the next input.

After all 9 inputs are processed, write `scenarios/icp-classification/evals/v001-raw.json` with this structure:

```json
{
  "prompt_version": "v001",
  "timestamp": "[execution timestamp]",
  "config": {
    "target_model": "haiku",
    "prompt_file": "prompts/v001.md"
  },
  "results": [
    {
      "input_id": "abacus-ai",
      "split": "train",
      "haiku_output": { ... parsed JSON or null ... },
      "raw_response": "... raw text from agent ...",
      "parse_failure": false,
      "missing_fields": []
    }
  ],
  "summary": {
    "total_inputs": 9,
    "successful_parses": N,
    "parse_failures": N,
    "missing_field_count": N
  }
}
```

The `split` field for each input comes from the ground truth file's `split` value. The 5 train inputs are: abacus-ai, bartos-industries, dbs-building-solutions, dealer-teamwork, osp. The 4 validation inputs are: roush-cleantech, title-one, trx-services, wisconsin-plastics.

IMPORTANT: Process inputs in alphabetical order by id. If an agent call fails entirely (not just parse failure but actual agent error), record it with `parse_failure: true` and `raw_response: "AGENT_ERROR: [error message]"` and continue.
  </action>
  <verify>
    <automated>cat scenarios/icp-classification/evals/v001-raw.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'Results: {len(d[\"results\"])}, Parses: {d[\"summary\"][\"successful_parses\"]}, Failures: {d[\"summary\"][\"parse_failures\"]}')"</automated>
  </verify>
  <acceptance_criteria>
    - evals/v001-raw.json exists and is valid JSON
    - evals/v001-raw.json contains exactly 9 entries in the "results" array
    - Each result has fields: input_id, split, haiku_output, raw_response, parse_failure, missing_fields
    - The 5 train inputs have split="train": abacus-ai, bartos-industries, dbs-building-solutions, dealer-teamwork, osp
    - The 4 validation inputs have split="validation": roush-cleantech, title-one, trx-services, wisconsin-plastics
    - summary.total_inputs equals 9
    - summary.successful_parses + summary.parse_failures equals 9
    - No Python/Node crash occurred during execution (all 9 inputs attempted)
  </acceptance_criteria>
  <done>All 9 inputs processed through Haiku via agent spawning. Raw outputs stored with parse status. Zero crashes regardless of Haiku output quality.</done>
</task>

</tasks>

<verification>
1. `ls scenarios/icp-classification/prompts/v001.md` — file exists
2. `ls scenarios/icp-classification/mutations.log` — file exists
3. `ls scenarios/icp-classification/evals/v001-raw.json` — file exists
4. v001-raw.json has 9 results with correct split assignments
5. mutations.log has v001 initial entry
</verification>

<success_criteria>
- v001.md is a complete, self-contained candidate prompt that can be passed to Haiku with any company input appended
- All 9 inputs have been processed through Haiku and raw outputs captured
- Parse failures (if any) are logged, not crashed
- v001-raw.json is ready for the judge in Plan 02
- mutations.log format is established for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-execution-evaluation/02-01-SUMMARY.md`
</output>
