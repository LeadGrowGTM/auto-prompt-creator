---
phase: 01-scenario-foundation-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenarios/icp-classification/scenario.json
  - scenarios/icp-classification/inputs/dbs-building-solutions.json
  - scenarios/icp-classification/inputs/trx-services.json
  - scenarios/icp-classification/inputs/osp.json
  - scenarios/icp-classification/inputs/bartos-industries.json
  - scenarios/icp-classification/inputs/roush-cleantech.json
  - scenarios/icp-classification/inputs/wisconsin-plastics.json
  - scenarios/icp-classification/inputs/abacus-ai.json
  - scenarios/icp-classification/inputs/dealer-teamwork.json
  - scenarios/icp-classification/inputs/title-one.json
  - scenarios/icp-classification/README.md
autonomous: true
requirements:
  - SCEN-01
  - SCEN-02
  - SCEN-03

must_haves:
  truths:
    - "A scenario directory exists with scenario.json that defines task, schema, rubric, and config"
    - "Individual input files exist in inputs/ subdirectory with company data"
    - "Output schema in scenario.json is dynamic -- field names come from the task, not hardcoded system logic"
    - "Scenario directory is fully self-contained and portable"
  artifacts:
    - path: "scenarios/icp-classification/scenario.json"
      provides: "Scenario definition with task, input_schema, output_schema, rubric, config, label_definitions"
      contains: "icp-classification"
    - path: "scenarios/icp-classification/inputs/dbs-building-solutions.json"
      provides: "Example input file"
      contains: "DBS Building Solutions"
    - path: "scenarios/icp-classification/README.md"
      provides: "Scenario status and metadata"
      contains: "icp-classification"
  key_links:
    - from: "scenarios/icp-classification/scenario.json"
      to: "scenarios/icp-classification/inputs/*.json"
      via: "input_schema defines the shape that input files conform to"
      pattern: "input_schema"
---

<objective>
Create the ICP classification scenario directory with scenario.json definition and all 9 company input files.

Purpose: Establish the file format conventions and first real scenario that Phase 1's ground truth generation (Plan 02) and Phase 2's execution engine will consume. Per D-02, every scenario is a self-contained directory.

Output: Complete scenario directory with validated scenario.json and 9 input files ready for ground truth generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scenario-foundation-ground-truth/01-CONTEXT.md
@.planning/phases/01-scenario-foundation-ground-truth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scenario.json with full schema definition</name>
  <files>
    scenarios/icp-classification/scenario.json
  </files>
  <read_first>
    .planning/phases/01-scenario-foundation-ground-truth/01-RESEARCH.md
    .planning/phases/01-scenario-foundation-ground-truth/01-CONTEXT.md
  </read_first>
  <action>
Create the directory structure: `scenarios/icp-classification/` with empty subdirectories `inputs/`, `ground-truth/`, `prompts/`, `evals/`, `library/`.

Create `scenarios/icp-classification/scenario.json` using the EXACT schema from the RESEARCH.md "scenario.json -- Complete Schema" section. The file must contain:

Per D-01, D-03: JSON format with these top-level keys:
- `name`: `"icp-classification"`
- `description`: `"Classify companies into ICP segments from company descriptions using casual, normalized business language"`
- `created`: `"2026-03-30"`
- `version`: 1

Per D-03 config block:
- `config.target_model`: `"haiku"`
- `config.accuracy_threshold`: 0.92
- `config.max_iterations`: 15
- `config.token_budget`: 800
- `config.train_validation_split`: 0.6 (per D-09)
- `config.split_seed`: `"icp-classification"` (per D-10)

Per D-03 input_schema:
- `company_name`: `{"type": "string", "required": true}`
- `company_description`: `{"type": "string", "required": true}`
- `website_url`: `{"type": "string", "required": false}`

Per D-04 output_schema (dynamic -- describes shape, not rigid validation):
- `industry`: type string, description "Casual industry label (e.g., 'commercial cleaning' not 'janitorial services')"
- `company_size`: type string, description "Casual size label", allowed_values ["startup", "SMB", "mid-market", "enterprise"]
- `decision_makers`: type "string[]", description "Casual titles (e.g., 'CMOs' not 'Chief Marketing Officers')"
- `pain_points`: type "string[]", description "Specific to this company, not generic"
- `icp_fit`: type string, allowed_values ["strong", "moderate", "weak"]
- `reasoning`: type string, description "One sentence explaining the fit classification"

Per D-14 rubric with 4 dimensions:
- `accuracy`: weight 0.4
- `label_normalization`: weight 0.25
- `completeness`: weight 0.2
- `specificity`: weight 0.15
- `scoring`: "1-5 per dimension, weighted average normalized to 0-1. 5 = indistinguishable from Opus output. 1 = wrong or missing."

Include `label_definitions` section with canonical values for `company_size` (startup/SMB/mid-market/enterprise with employee thresholds) and `icp_fit` (strong/moderate/weak with matching criteria). Use the exact definitions from the RESEARCH.md code example.
  </action>
  <verify>
    <automated>cat scenarios/icp-classification/scenario.json | python -m json.tool > /dev/null 2>&1 && echo "VALID JSON" || echo "INVALID JSON"</automated>
  </verify>
  <acceptance_criteria>
    - scenarios/icp-classification/scenario.json is valid JSON (python -m json.tool succeeds)
    - File contains key "name" with value "icp-classification"
    - File contains key "config" with nested "target_model", "accuracy_threshold", "train_validation_split", "split_seed"
    - config.accuracy_threshold equals 0.92
    - config.train_validation_split equals 0.6
    - File contains "output_schema" with keys: industry, company_size, decision_makers, pain_points, icp_fit, reasoning
    - output_schema.company_size contains "allowed_values" array with exactly ["startup", "SMB", "mid-market", "enterprise"]
    - File contains "rubric" with "dimensions" containing accuracy (weight 0.4), label_normalization (0.25), completeness (0.2), specificity (0.15)
    - File contains "label_definitions" with company_size and icp_fit definitions
    - Directories exist: scenarios/icp-classification/inputs/, ground-truth/, prompts/, evals/, library/
  </acceptance_criteria>
  <done>scenario.json validates as JSON with all required sections: name, description, config, input_schema, output_schema, rubric, label_definitions. All subdirectories created.</done>
</task>

<task type="auto">
  <name>Task 2: Create 9 company input files and README</name>
  <files>
    scenarios/icp-classification/inputs/dbs-building-solutions.json
    scenarios/icp-classification/inputs/trx-services.json
    scenarios/icp-classification/inputs/osp.json
    scenarios/icp-classification/inputs/bartos-industries.json
    scenarios/icp-classification/inputs/roush-cleantech.json
    scenarios/icp-classification/inputs/wisconsin-plastics.json
    scenarios/icp-classification/inputs/abacus-ai.json
    scenarios/icp-classification/inputs/dealer-teamwork.json
    scenarios/icp-classification/inputs/title-one.json
    scenarios/icp-classification/README.md
  </files>
  <read_first>
    scenarios/icp-classification/scenario.json
    .planning/phases/01-scenario-foundation-ground-truth/01-RESEARCH.md
    .planning/phases/01-scenario-foundation-ground-truth/01-CONTEXT.md
  </read_first>
  <action>
Create 9 input files in `scenarios/icp-classification/inputs/`, one per company. Each file is a JSON object conforming to the input_schema from scenario.json.

Each input file MUST have this exact structure:
```json
{
  "id": "{kebab-case-slug}",
  "company_name": "{Company Name}",
  "company_description": "{2-4 sentence description with industry, size indicators, pain points, tech stack mentions}",
  "website_url": "{url or null}"
}
```

The 9 companies (from the original conversation and RESEARCH.md references):

1. **dbs-building-solutions.json** -- Commercial cleaning and facility maintenance, 340 employees, $28M revenue, Midwest, ServiceTitan for dispatch, QA scaling struggles
2. **trx-services.json** -- B2B field services company (write a realistic 2-4 sentence description covering services, approximate size, and operational challenges)
3. **osp.json** -- Office/business services provider (write a realistic 2-4 sentence description)
4. **bartos-industries.json** -- Industrial/manufacturing company (write a realistic 2-4 sentence description)
5. **roush-cleantech.json** -- Clean energy / alternative fuel vehicles, enterprise-scale (write a realistic description reflecting their actual business: alternative fuel vehicle systems)
6. **wisconsin-plastics.json** -- Plastics manufacturing, mid-market (write a realistic description)
7. **abacus-ai.json** -- AI/ML platform company, tech/SaaS (write a realistic description reflecting their actual business: enterprise AI platform)
8. **dealer-teamwork.json** -- Automotive marketing tech/SaaS (write a realistic description reflecting their actual business: AI-powered automotive marketing platform)
9. **title-one.json** -- Education services (write a realistic description)

IMPORTANT per Pitfall 2 in RESEARCH.md: Ensure diversity across:
- Industry: cleaning, field services, office services, manufacturing, clean energy, plastics, AI/SaaS, auto marketing tech, education
- Size: include at least 1 startup/SMB, several mid-market, at least 1 enterprise
- ICP fit: at least 1-2 should be WEAK fit companies (likely abacus-ai and/or title-one since they're tech/education, not typical B2B services)

For DBS Building Solutions specifically, use the description from RESEARCH.md verbatim:
"Commercial cleaning and facility maintenance company serving corporate offices, medical facilities, and industrial sites across the Midwest. 340 employees, $28M revenue. Uses ServiceTitan for dispatch and has struggled to scale their quality assurance process."

For all others, write company descriptions that are realistic, contain enough detail for ICP classification, and use the casual language style per D-13.

Also create `scenarios/icp-classification/README.md` with:
- One-sentence description of what this scenario optimizes
- Status: "defining" (will progress to "generating GT" then "optimizing" then "graduated")
- Input count: 9
- Current accuracy: N/A
- Best prompt version: N/A
- Created date: 2026-03-30
  </action>
  <verify>
    <automated>ls scenarios/icp-classification/inputs/*.json | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 9 .json files exist in scenarios/icp-classification/inputs/
    - Each file is valid JSON (python -m json.tool succeeds on each)
    - Each file contains keys: id, company_name, company_description
    - dbs-building-solutions.json contains "340 employees"
    - dbs-building-solutions.json contains "ServiceTitan"
    - At least one input has company_description suggesting a tech/SaaS company (diversity check)
    - At least one input has company_description suggesting < 50 employees (size diversity)
    - scenarios/icp-classification/README.md exists and contains "icp-classification"
    - README.md contains "Status:" line
  </acceptance_criteria>
  <done>9 input files exist with diverse company profiles covering multiple industries, sizes, and ICP fit levels. README.md documents scenario status.</done>
</task>

</tasks>

<verification>
1. `ls scenarios/icp-classification/` shows: scenario.json, README.md, inputs/, ground-truth/, prompts/, evals/, library/
2. `cat scenarios/icp-classification/scenario.json | python -m json.tool` succeeds (valid JSON)
3. `ls scenarios/icp-classification/inputs/*.json | wc -l` returns 9
4. All input files parse as valid JSON
5. scenario.json contains all required sections: name, config, input_schema, output_schema, rubric, label_definitions
</verification>

<success_criteria>
- Complete scenario directory structure exists with all 6 subdirectories
- scenario.json defines the full schema per locked decisions D-01 through D-04, D-09, D-10, D-14
- 9 company input files with diverse profiles ready for ground truth generation
- README.md tracks scenario status
- Directory is fully self-contained -- no references to external files or config
</success_criteria>

<output>
After completion, create `.planning/phases/01-scenario-foundation-ground-truth/01-01-SUMMARY.md`
</output>
