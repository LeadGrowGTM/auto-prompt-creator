---
phase: 03-mutation-optimization-loop
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md
  - .planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md
autonomous: true
requirements:
  - MUT-01
  - MUT-02
  - MUT-03

must_haves:
  truths:
    - "Opus has a concrete prompt template for reading eval JSON and producing structured failure taxonomy JSON"
    - "Failure taxonomy always produces named patterns with frequency, affected inputs, and a suggested rule"
    - "Opus has a concrete prompt template for transforming vN into v(N+1) with a single focused mutation"
    - "Additive vs consolidation vs subtractive mutation type is explicitly chosen and logged in the prompt"
  artifacts:
    - path: ".planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md"
      provides: "Opus prompt for failure analysis — reads eval JSON, produces failure_analysis JSON"
      contains: "failure_analysis, patterns array, priority_target"
    - path: ".planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md"
      provides: "Opus prompt for generating the next prompt version from failure analysis"
      contains: "additive, consolidation, subtractive, single focused change"
  key_links:
    - from: "failure-analysis-prompt.md"
      to: "evals/vNNN.json"
      via: "reads per_input scores and judge_rationales"
      pattern: "per_input"
    - from: "mutation-instruction-prompt.md"
      to: "prompts/vNNN.md"
      via: "current prompt is input, new prompt is output"
      pattern: "prompts/v"
---

<objective>
Write the two Opus prompt templates that power the mutation engine: (1) failure analysis — reads a scored eval and produces structured failure taxonomy JSON, (2) mutation instruction — transforms the current prompt into the next version with one targeted change.

Purpose: These are the intelligence layer of the anneal loop. Without concrete Opus prompts, the loop has no methodology — just a script with no instructions on what to think or produce.
Output: Two markdown prompt files under .planning/phases/03-mutation-optimization-loop/prompts/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mutation-optimization-loop/03-CONTEXT.md
@.planning/phases/03-mutation-optimization-loop/03-RESEARCH.md

<interfaces>
<!-- Failure analysis output schema (from RESEARCH.md) — mutation-instruction-prompt reads this structure -->
```json
{
  "iteration": "v001",
  "overall_score": 0.6403,
  "train_score": 0.655,
  "validation_score": 0.6219,
  "patterns": [
    {
      "name": "strong-fit-bias",
      "frequency": 5,
      "severity": "critical",
      "affected_inputs": ["abacus-ai", "bartos-industries", "dealer-teamwork", "osp", "roush-cleantech"],
      "affected_dimensions": ["accuracy"],
      "description": "Haiku defaults to 'strong' icp_fit when ground truth says moderate or weak.",
      "evidence": "abacus-ai: strong vs weak. osp: strong vs moderate.",
      "suggested_rule": "Add explicit disqualifying criteria for icp_fit with a concrete decision tree."
    }
  ],
  "priority_target": "strong-fit-bias"
}
```

<!-- v001 prompt (current baseline — mutation-instruction-prompt will receive this as input) -->
From scenarios/icp-classification/prompts/v001.md:
- ~270 tokens, structured with ## Output Format, ## Rules, ## Company to Classify sections
- icp_fit criteria currently: "strong = matches on industry + size + pain points for B2B service outbound. moderate = matches 1-2 dimensions. weak = does not match key dimensions."
- This vague criteria is the root cause of strong-fit-bias

<!-- Known failure patterns from v001.json (concrete, not abstract) -->
Primary: strong-fit-bias — 5/9 inputs wrongly classified as strong (abacus-ai, osp, roush-cleantech, title-one) or wrong direction (dealer-teamwork moderate vs weak)
Secondary: formal-language-regression — 4/9 inputs use formal title-case (VP of Sales, Operations Manager) vs ground truth casual (sales leads, ops directors)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write failure-analysis-prompt.md</name>
  <files>.planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md</files>
  <read_first>
    - .planning/phases/03-mutation-optimization-loop/03-RESEARCH.md (Pattern: Failure Analysis Structure section, Baseline Failure Analysis section)
    - scenarios/icp-classification/evals/v001.json (real data this prompt will analyze — full per_input array with judge_rationales)
    - scenarios/icp-classification/scenario.json (rubric dimensions and weights — accuracy 0.4, label_normalization 0.25, completeness 0.2, specificity 0.15)
    - .planning/phases/03-mutation-optimization-loop/03-CONTEXT.md (D-01, D-02, D-03 for output structure requirements)
  </read_first>
  <action>
Create .planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md

This is the prompt Opus reads during the anneal loop to analyze a scored eval and produce structured failure taxonomy JSON. It gets executed by Opus (the current session) with the actual eval JSON injected at the {{EVAL_JSON}} placeholder.

The prompt file must contain the following content (write this exactly into the file):

[BEGIN PROMPT FILE CONTENT]

You are a prompt optimization analyst. Your job is to analyze scored evaluation results and identify the highest-priority failure patterns to target in the next mutation.

## Task

Analyze the eval results provided below. For each input where Haiku scored below the accuracy threshold or had a per-dimension score below 0.75, compare the haiku_output fields against the judge_rationales to identify what went wrong.

Then aggregate across inputs: identify repeating patterns (same type of failure appearing in 2+ inputs) and assign named taxonomy labels.

Taxonomy label examples (name these precisely, not generically):
- "strong-fit-bias" — Haiku defaults to strong icp_fit when ground truth says moderate or weak
- "formal-language-regression" — decision_makers or industry use formal title-case instead of casual lowercase
- "missing-field" — required field absent or contains placeholder
- "hallucinated-data" — output references facts not in the input description
- "wrong-buyer-persona" — decision_makers are wrong function (e.g., marketing vs engineering)
- "specificity-filler" — pain_points are generic, not tied to company-specific details

## Output Format

Return ONLY a JSON object matching this schema exactly:

```json
{
  "iteration": "[version string, e.g. v001]",
  "overall_score": [number],
  "train_score": [number],
  "validation_score": [number],
  "patterns": [
    {
      "name": "[taxonomy label]",
      "frequency": [integer — number of inputs affected],
      "severity": "critical | high | medium | low",
      "affected_inputs": ["[input_id]", ...],
      "affected_dimensions": ["[dimension name]", ...],
      "description": "[One sentence: what went wrong and why]",
      "evidence": "[Comma-separated examples: input_id: haiku_value vs ground_truth_value]",
      "suggested_rule": "[Concrete instruction that, if added to the prompt, would fix this pattern]"
    }
  ],
  "priority_target": "[name of the highest-frequency/highest-severity pattern to fix first]"
}
```

Severity rules:
- critical: affects accuracy dimension (weight 0.4) in 3+ inputs
- high: affects accuracy in 1-2 inputs OR affects another dimension in 3+ inputs
- medium: affects 1-2 inputs in non-accuracy dimensions
- low: minor stylistic issues in isolated cases

## Rules

- List patterns from most to least severe (critical first)
- Only include patterns appearing in 2+ inputs (isolated failures are noise, not patterns)
- The suggested_rule must be actionable — a concrete instruction Opus can write into the prompt, not a vague description
- Do not include generic observations like "model needs improvement" — only named, specific patterns
- priority_target must be a pattern name from the patterns array

## Eval Data

{{EVAL_JSON}}

[END OF PROMPT FILE CONTENT]

Note: When this prompt is used in the anneal loop, replace {{EVAL_JSON}} with the full contents of the current iteration's evals/vNNN.json file.
  </action>
  <verify>
    <automated>grep -c "priority_target" ".planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md
    - Contains "{{EVAL_JSON}}" placeholder string
    - Contains "priority_target" in the output schema
    - Contains "suggested_rule" field in the patterns array schema
    - Lists at least 4 taxonomy label examples (strong-fit-bias, formal-language-regression, missing-field, hallucinated-data)
    - Contains severity definitions (critical, high, medium, low)
    - Contains the rule "Only include patterns appearing in 2+ inputs"
  </acceptance_criteria>
  <done>failure-analysis-prompt.md is a complete, concrete Opus prompt that, when given an eval JSON, produces structured failure taxonomy ready to feed the mutation engine</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write mutation-instruction-prompt.md</name>
  <files>.planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md</files>
  <read_first>
    - .planning/phases/03-mutation-optimization-loop/03-CONTEXT.md (D-05 through D-11 for mutation strategy rules)
    - .planning/phases/03-mutation-optimization-loop/03-RESEARCH.md (Pattern: Mutation Log Entry Format, Anti-Patterns to Avoid, Pitfall 1 — Strong-Fit Bias Persistence)
    - scenarios/icp-classification/prompts/v001.md (the baseline prompt that will be mutated — executor needs to see its structure to write accurate instructions)
    - .planning/phases/03-mutation-optimization-loop/prompts/failure-analysis-prompt.md (read after Task 1 completes — mutation prompt consumes failure analysis output)
  </read_first>
  <action>
Create .planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md

This is the prompt Opus reads to generate the next prompt version from the failure analysis. It gets executed with three inputs injected: {{CURRENT_PROMPT}}, {{FAILURE_ANALYSIS}}, and {{LOOP_STATE}}.

Write the following content into the file exactly:

[BEGIN PROMPT FILE CONTENT]

You are a prompt engineer optimizing a Haiku prompt using SIMBA-style self-reflection. Your job is to make one focused, targeted change to the current prompt that addresses the highest-priority failure pattern.

## Inputs You Have

- CURRENT_PROMPT: The prompt version being improved (with its current token estimate)
- FAILURE_ANALYSIS: Structured JSON identifying failure patterns from the latest eval
- LOOP_STATE: Current loop state including score history, consecutive_plateaus, and token count

## Mutation Type Decision

Before writing the mutation, decide the mutation type:

**additive** (default): Append a targeted corrective rule to the most relevant section of the prompt.
- Use when: consecutive_plateaus < 2 AND estimated tokens < 700
- Best for: Fixing a named failure pattern by adding explicit criteria or examples

**consolidation** (rewrite for conciseness): Rewrite the full prompt to be more concise while preserving all active rules.
- Use when: consecutive_plateaus >= 2 with additive-only mutations OR estimated tokens >= 700
- Goal: Reduce token count by 15-25% while keeping all behavioral rules intact
- After consolidation: re-run eval before continuing additive mutations

**subtractive** (remove/simplify): Remove or simplify a specific instruction that is bloating the prompt.
- Use when: Estimated tokens >= 750 and consolidation has already been run
- Only remove instructions that are redundant or that evidence shows aren't helping

## Mutation Rules

1. **Single focus**: One failure pattern per mutation. Do not fix two things at once.
2. **Target the priority_target**: From the failure analysis, use the highest-priority pattern.
3. **Be concrete**: Haiku follows examples better than abstract criteria. Include a concrete decision example (e.g., "A company with government procurement cycles is moderate, not strong — even if it has sales pain").
4. **Preserve what works**: Check current dimension scores. completeness is 0.972 — do not touch instructions that affect it. specificity is 0.694 — do not weaken it.
5. **One section**: For additive mutations, append to the ## Rules section or create a focused sub-section. Do not restructure the prompt.
6. **No regressions**: The mutation must not remove existing rules that address other patterns.

## ICP Fit Guidance (for strong-fit-bias pattern)

If the priority_target is "strong-fit-bias", the suggested rule must include:
- Explicit disqualifying factors that prevent defaulting to strong
- Decision criteria framed as: "Before classifying strong, check: does the company have [blocking factor]? If yes, use moderate or weak."
- Blocking factors from the v001 failure data: government/public sector procurement, SaaS business model (not a B2B service company), capacity-constrained operations, no existing sales infrastructure need, wrong buyer persona (technical vs commercial)
- At minimum one concrete example: a company type that LOOKS like a strong fit but is actually moderate or weak

## Label Normalization Guidance (for formal-language-regression pattern)

If the priority_target is "formal-language-regression":
- Add a DO/DON'T examples list for decision_makers titles
- DO: "sales leads", "ops directors", "the founder", "biz dev", "marketing directors"
- DON'T: "VP of Sales", "Operations Manager", "Business Development Manager", "Founder/CEO"

## Output Format

Return two things:

### 1. New Prompt (complete text)

The full text of the new prompt version. Start with the current prompt, apply the single mutation, return the complete revised text. Never return a diff or partial prompt.

### 2. Mutation Log Entry

A structured entry to append to mutations.log in this exact format:

```
## v[CURRENT] -> v[NEXT] ([ISO timestamp])
Target: [pattern name] ([frequency]/9 inputs affected)
Type: [additive | consolidation | subtractive]
Mutation: [One paragraph: what was added/changed and exactly where in the prompt]
Token count: v[CURRENT]=[N] -> v[NEXT]=[estimated N]
Score delta: [current score] -> [TBD — measured after run]
Train: [TBD] | Validation: [TBD]
```

## Inputs

{{CURRENT_PROMPT}}

{{FAILURE_ANALYSIS}}

{{LOOP_STATE}}

[END OF PROMPT FILE CONTENT]

Note: When this prompt is used in the anneal loop, replace all three {{placeholders}} with their actual values before executing.
  </action>
  <verify>
    <automated>grep -c "{{CURRENT_PROMPT}}" ".planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/03-mutation-optimization-loop/prompts/mutation-instruction-prompt.md
    - Contains all three placeholders: "{{CURRENT_PROMPT}}", "{{FAILURE_ANALYSIS}}", "{{LOOP_STATE}}"
    - Contains "additive", "consolidation", "subtractive" mutation type definitions
    - Contains the rule "Single focus: One failure pattern per mutation"
    - Contains ICP Fit Guidance section with blocking factors
    - Contains Label Normalization Guidance section with DO/DON'T examples
    - Contains "## Mutation Log Entry" output format with the exact mutations.log structure
    - Mutation log entry format contains "Token count:" and "Score delta:" lines
  </acceptance_criteria>
  <done>mutation-instruction-prompt.md provides Opus with concrete, decision-tree-based instructions for generating one targeted prompt mutation per iteration, including mutation log entry format ready to append to mutations.log</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Both prompt files exist under .planning/phases/03-mutation-optimization-loop/prompts/
2. failure-analysis-prompt.md contains {{EVAL_JSON}} placeholder and full output schema
3. mutation-instruction-prompt.md contains all three {{placeholders}} and both ICP Fit and Label Normalization guidance sections
4. The two prompts are linked: failure-analysis output feeds mutation-instruction input
</verification>

<success_criteria>
An Opus session executing the anneal loop can inject eval JSON into failure-analysis-prompt.md and get structured taxonomy JSON back, then inject that taxonomy + current prompt + loop state into mutation-instruction-prompt.md and get a complete new prompt version plus mutations.log entry back.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mutation-optimization-loop/03-02-SUMMARY.md`
</output>
